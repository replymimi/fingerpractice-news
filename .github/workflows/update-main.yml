name: 財經晨讀主更新

on:
  schedule:
    - cron: '15 22 * * *'  # 06:15 台北時間觸發，目標 07:00 前上線
  workflow_dispatch:

permissions:
  contents: write

env:
  run_type: main

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 檢查倉庫代碼
        uses: actions/checkout@v4

      - name: 設定 Python 環境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安裝套件
        run: pip install -r requirements.txt

      - name: 抓新聞 RSS
        run: python scripts/fetch_news.py

      - name: 抓大盤指數
        run: python scripts/fetch_market.py

      - name: 抓潮汐板塊資料
        run: python scripts/fetch_tide.py

      - name: 抓 YouTube 影片（除游庭皓外全部）
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          SUPADATA_API_KEY: ${{ secrets.SUPADATA_API_KEY }}
        run: python scripts/fetch_youtube.py --run-type main

      - name: AI 翻譯與摘要
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts/summarize.py

      - name: 產生網頁
        run: python scripts/build_page.py

      - name: 提交更新
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/raw data/cache data/state docs/index.html
          git diff --cached --quiet || git commit -m "🤖 自動更新財經晨讀（main）"
          for i in 1 2 3 4 5; do
            git push origin main && exit 0
            echo "push rejected, pulling and retrying ($i/5)"
            git pull --rebase origin main
            sleep $((RANDOM % 10 + 1))
          done
          git push origin main
